experiment:
  language: "anbn"
  output_dir: "outputs/experiments/exp1_length"
  stratified_test: true
  difficulty_test: false
  pairs_per_bucket: 50

training:
  train_pairs: 500
  train_min_n: 1
  train_max_n: 100
  epochs: 10
  learning_rate: 0.01
  seed: 42

model:
  hidden_size: 32
  beta: 0.85
  learn_beta: true

device:
  device: null
